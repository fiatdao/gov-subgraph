{
  "name": "fiat-dao-gov-subgraph",
  "version": "0.1.0",
  "scripts": {
    "build": "GRAPH=dev.json NO_DEPLOY=true ./deploy.sh",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.21.1",
    "@graphprotocol/graph-ts": "^0.20.1",
    "mustache": "^4.2.0"
  }
}
